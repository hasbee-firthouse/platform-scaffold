=== Session 1 ===
date: 2026-07-16T19:55:00+05:30
mode: full
pipeline_position: BUILD COMPLETE + RUNTIME INTEGRATION COMPLETE — awaiting live /evaluate (Docker)
groups_completed: [A, B, C, D, E, F, G, H, I, J]
groups_remaining: []
current_group: none — build + integration wiring done; next is a LIVE evaluate (needs Docker)
current_stories: []
sprint_contract: none
last_commit: 24d7050 fix(deletability): platform tests module-agnostic (post-integration)
features_passing: 82 / 95 (13 deferred are all live-stack; the integration below makes them
  genuinely reachable in a live /evaluate — they are NOT yet marked pass, pending live verify)
coverage: 820 tests green across 140 files; tsc + eslint clean; vite build passes
integration_complete (commits e21e6b9, 9aff10c, 7024707, 24d7050 — closes the build->evaluate gap):
  - API boot fully wired: pg-boss workers + email port started at boot; reference module mounted
    via the deletion-safe modules/register-apis.ts seam; ctx.entitlements/permissions merged from
    MODULE_MANIFESTS; better-auth verify/reset/magic-link + invite + set-password emails go through
    ctx.email; POST /api/orgs/:orgId/members added; /api/me uses @platform/authz. SMTP_URL added.
  - SPA fully assembled: TanStack router wires all screens (auth/settings/org-switcher/workspace)
    with a /api/me session bootstrap feeding <Can>/useTerm; module web routes via modules/
    register-web.ts seam. vite build succeeds (navigable end-to-end).
  - Deploy: scripts/init-db.sql (app_runtime role) + db-bootstrap.ts + entrypoint.sh
    (init->migrate->boot); compose mounts init + app_runtime; two-role model in DEPLOYMENT.md.
    Request pool stays on DATABASE_URL (owner) for now — per-repo pool routing to app_runtime is
    the documented prod follow-up; RLS backstop is still proven by the isolation suite.
  - Deletability PRESERVED + re-verified after all the churn: deleting the module (dir + lines in
    modules/index.ts, register-apis.ts, register-web.ts + drizzle.config.ts) leaves tsc clean, a
    clean drop migration, vite build passing, and 675 tests green (0 failures).
build_complete_note:
  - E8-S5 (group J): multi-stage Dockerfile (non-root, SPA build, tsx boot serving API+SPA :3000),
    docker-compose (postgres16 + mailpit + opt-in app), dev-only scripts/seed.ts (assertNotProduction
    guard), docs/FORKING.md (SPEC §24) + DEPLOYMENT.md, root seed script.
  - DELETABILITY (F094) VERIFIED by the orchestrator: actually deleted modules/reference-workspace +
    emptied the registry + removed the drizzle schema entry -> root tsc clean, db:generate emits a
    clean drop migration, 620 platform tests pass (0 fail); then restored. Fixed two real deletability
    defects found in the process: (1) ModuleManifest/ModuleJob types moved from the module to the
    platform-owned modules/index.ts (primitive-typed, zero deps) so the registry survives deletion;
    (2) modules/index.test.ts made deletability-safe (generic registry-mechanism test; the
    'reference-workspace is registered' assertion moved into the module's own manifest.test.ts).
  - INCIDENT: an over-broad `git clean -fd` during the deletability test removed untracked EMPTY
    placeholder dirs (specs/brd, specs/design/amendments, specs/reviews, specs/test_artefacts,
    sprint-contracts) — never git-tracked, no content lost per git history; recreated. Lesson: use
    targeted `rm` for test artifacts, never `git clean -fd`, when uncommitted/untracked work exists.
group_I_stories:
  - E8-S4 (export job): modules/reference-workspace/api/export.job.ts (workspace.export job:
    Zod payload, pure buildTasksCsv / RFC-4180 csv.ts, handler loads tasks via withOrg + email.send)
    + plugin.ts POST .../workspaces/:id/export (202, enqueues) + manifest jobs field with
    retry {retryLimit:3, retryDelay:5, retryBackoff:true}. F091 (live Mailpit delivery) deferred.
  - E8-S3 (workspace web screens): modules/reference-workspace/web/ — workspaces.screen,
    tasks.screen (add/complete/uncomplete/delete, open/done filter, native-select assignee picker
    over org members, upgrade notice on 403 ENTITLEMENT_REQUIRED), workspace-client, terminology,
    upgrade-notice. useTerm nouns, token-only (no hex), no better-auth. Module package gained
    react/ui/query/testing-library. NOTE: component-map's routes.tsx + nav.ts were NOT created
    (agent was session-limited before the final wiring glue) — screens are standalone + fully
    tested, so F087-F089 hold; routes.tsx/nav.ts belong with the deferred SPA-router wiring.
  - E6-S3 (isolation & migration suite): apps/api/test/{isolation,rls-direct-sql,migration-
    idempotency}.spec.ts — LIVE-DB harnesses, gated by .spec.ts extension (not in default run) +
    describe.skipIf(!TEST_DATABASE_URL / !TEST_RUNTIME_DATABASE_URL). vitest.isolation.config.ts +
    root "test:isolation" script for evaluate. F067/F068/F069 deferred (run in evaluate against a
    migrated Postgres with the app_runtime role). Default suite unchanged (21 specs skip cleanly).
group_H_stories:
  - E6-S2 (RLS backstop): @platform/tenancy rls.ts SQL generators (FORCE RLS + CREATE POLICY
    USING(org_id = current_setting('app.org_id',true)) TEXT predicate — org ids are better-auth
    text, NOT uuid; F014/AC-wording precedent) + runtime GRANTs to a parameterized `app_runtime`
    role. Migration 0005_rls_backstop_policies.sql covers workspace, task, entitlement_override,
    audit_log (audit_log policy also allows org_id IS NULL for system rows). Two-role model
    documented (runtime=non-owner/NOBYPASSRLS, migrations=privileged owner) — roles created by
    deploy bootstrap (E10), not here. F065/F066 (runtime can't bypass; cross-tenant zero rows)
    DEFERRED to the live isolation suite. Hand-authored migration (drizzle-kit can't emit FORCE
    RLS/GRANTs); no schema drift.
  - E8-S2 (workspace/task services): modules/reference-workspace/api/ — plugin.ts mounts
    /api/orgs/:orgId/workspace/*, workspace.service + task.service (pure/injectable), repository.ts
    (the ONLY file with workspace/task queries — all wrapped in withOrg, so E6-S1 guard stays
    green + RLS engages), authz.ts (per-route perms via createPermissionRegistry([manifest]) +
    the authorizeOrg role-resolution pattern), errors/views/deps. Task assignee must be a current
    member (else 422); task-count >= maxTasks -> 403 ENTITLEMENT_REQUIRED; workspace created/deleted
    audited. Added fastify/type-provider/tenancy/audit/contracts deps to the module package.json.
    NOT yet wired into apps/api app.ts (integration follow-up). Live end-to-end/isolation DEFERRED.
group_G_stories:
  - E6-S1 (tenancy): @platform/tenancy withOrg (txn + set_config('app.org_id',$1,true),
    parameterized/pooler-safe) + requireOrgMembership preHandler (404 non-member, injectable
    resolver) + AC3 guard test scoped to modules/** (enforces module tenant queries go through
    withOrg; control-plane/audit/entitlement seams in packages/** are exempt). 12 tests.
    NOT YET wired onto real routes (E5-S2 routes still use inline authorizeOrg) — integration debt.
  - E8-S1 (reference module): modules/reference-workspace (@module/reference-workspace) —
    workspace+task drizzle schema (migration 0004), Zod contracts (open|done), ModuleManifest
    (perms workspace.workspaces.manage / tasks.read / tasks.write, Workspace Manager role,
    workspace.maxTasks=100) registrable with authz+entitlements registries; modules/index.ts
    registry. drizzle.config.ts schema now an array incl the module. F082 deferred: contracts
    are OpenAPI-convertible but need module ROUTES (E8-S2/S3) to appear in live /api/docs.
  - E7-S2 (ops CLI): @ops/cli (ops/) — entitlement set/get/list, org list/restore (30-day
    window), user deactivate, invite resend; every mutation audits actorUserId 'system:cli'.
    Added ops to pnpm-workspace.yaml + vitest include + root "ops" script. 31 tests.
  - E5-S3 (org UI): API roles route (GET /api/orgs/:orgId/roles) done earlier; web part —
    org-switcher (shown only >1 org, links /o/:slug), settings-org general/members/invitations/
    roles screens + lib/org-client. Screens are standalone components (props-driven), NOT
    router-wired. 40 tests. Uses native <select> for role (jsdom-reliable vs Radix).
learned_rules: 1 (LR-001)
blocked_stories: none
group_F_status:
  DONE + committed (part 1):
    - E5-S2 (orgs & membership): schema/auth.ts +type/+deleted_at; migration 0002; custom
      /api/orgs routes (create/update/delete/transfer/members/invitations) on ctx.db via a
      Repository seam; at-least-one-owner invariant; owner-only soft-delete w/ name confirm;
      invite lifecycle (7-day token = invitation id) send/resend/revoke/accept; personal-org
      auto-create hook in better-auth (databaseHooks.user.create.after, gated on
      capabilities.personalAccounts). Invite email delivery deferred (injectable sendInvite).
    - E7-S1 (entitlements): schema/entitlement.ts (entitlement_override, uuid PK, jsonb value,
      RLS, unique org_id+key); migration 0003; @platform/entitlements (registry defaults +
      get/require); ctx.entitlements built in buildContext; GET /api/orgs/:orgId/entitlements;
      EntitlementRequiredError carries statusCode 403 + code (route translates, error-handler
      untouched); upgrade-notice CTA = mailto email.fromAddress / branding.supportUrl (added
      optional supportUrl to brandingSchema). No checkout.
  DONE + committed (part 2):
    - E4-S3 (auth screens): apps/web/src/screens/auth/* (sign-in/up, verify, forgot/reset,
      accept-invite, magic-link) + settings-user/security.screen + lib/auth-client.ts (fetch to
      /api/auth/*, NOT better-auth SDK; grep-guard still green). plugins/rate-limit.ts adds
      AUTH_RATE_LIMIT_MAX(10) + RateLimitedError; plugins/auth.ts applies it to /api/auth/* with
      a scoped 429 RATE_LIMITED handler (shared error-handler untouched). Failed-sign-in audit
      already covered by E2-S3 resolveAuthEvent — not duplicated. Added react-hook-form/
      @hookform/resolvers/zod/user-event to apps/web (were absent). Screens NOT router-wired
      (follow-up). 76 tests.
    - E7-S3 (audit viewer): platform-audit/viewer.ts listAuditLogs (org-scoped, action/actor
      filters, limit default 50 cap 100, {items,total}, newest-first); routes/orgs/audit-logs.ts
      GET admin-gated (org.settings.update via the E5-S2 authorizeOrg pattern — requirePermission
      needs a sync request.permissions the app doesn't attach, so used async role resolution
      instead; same 403 outcome); registered in app.ts; screens/settings-org/audit-log.screen.tsx
      <Can>-gated ui Table + filters + pagination. Screen NOT router-wired (follow-up). 38 tests.
integration_pending (carried debts — not blocking, wire when relevant):
  - Web screens (auth/*, settings-user, settings-org/*, org-switcher, reference-workspace/web/*)
    are unit-tested components NOT yet wired into the TanStack router / app.tsx. The reference
    module's web routes.tsx + nav.ts contribution files still need to be authored as part of that
    routing follow-up (E8-S3 delivered the screens but not those two wiring files).
  - Reference module (@module/reference-workspace) API plugin (registerWorkspaceModule) + its
    permission/entitlement registration NOT yet wired into apps/api app.ts. Wiring it makes F082
    (module contracts in live OpenAPI) verifiable and the workspace/task routes reachable.
  - withOrg + requireOrgMembership (E6-S1) not yet applied to the platform /api/orgs routes (they
    still use E5-S2 inline authorizeOrg). The reference module DOES use withOrg. Retrofit + the
    two-role RLS runtime (app_runtime) provisioning is the tenancy integration + deploy step.
  - MISSING endpoint: admin-create member POST /api/orgs/:orgId/members — E5-S3 members screen +
    org-client are wired to it but the server route doesn't exist yet (F059 UI unit-proven only).
  - jobs/email not wired into ctx/boot (start workers, ctx.email; invite + auth emails).
  - /api/me still uses its own local role→perms map; reconcile to @platform/authz resolveRole.
  - @platform/authz requirePermission preHandler still unused by real routes (org/audit routes
    use the async authorizeOrg pattern instead — requirePermission wants a sync request.permissions
    the app never attaches; reconcile or retire one).
deferred_features (need live stack / later stories): F021, F022, F091 (live pg-boss/Mailpit);
  F082 (wire module routes into app.ts for live OpenAPI); F065, F066, F067, F068, F069 (live RLS
  + isolation/migration suite with app_runtime role — run apps/api/test via `pnpm test:isolation`
  with TEST_DATABASE_URL + TEST_RUNTIME_DATABASE_URL). Re-verify live in evaluate:
  F026/F045/F046/F047/F048/F054/F057/F059/F061/F064/F072/F079/F083-F090/F092.
remaining_deferred_features (13 — all live-stack / evaluate-phase, none are build gaps):
  F012 F013 (migrations apply / drizzle connects — live DB), F014 (uuid-v7 PK convention — reconcile
  vs better-auth text-id core tables), F021 F022 (live pg-boss cron / Mailpit), F065 F066 F067 F068
  F069 (live RLS + isolation/migration suite — run `pnpm test:isolation` with TEST_DATABASE_URL +
  TEST_RUNTIME_DATABASE_URL), F082 (wire module plugin into app.ts for live OpenAPI), F091 (live
  Mailpit CSV), F093 (docker compose up + prod image boot).
integration_followups_for_evaluate (tracked; not build blockers):
  - Wire the reference module (registerWorkspaceModule + web routes.tsx/nav.ts + authz/entitlement
    registration + job workers) into apps/api app.ts and the SPA router. Also wire the other web
    screens (auth/*, settings-*, org-switcher) into the router.
  - Provision the app_runtime (non-owner/NOBYPASSRLS) role in the deploy bootstrap before migrations.
  - Reconcile /api/me local perms -> @platform/authz; retire the unused requirePermission preHandler
    OR attach request.permissions; add the missing admin-create-member POST /api/orgs/:orgId/members.
  - Wire jobs/email into ctx/boot (start pg-boss workers, ctx.email) for auth + invite + export mail.
EVALUATION RUNBOOK (Docker is NOT available in the agent sandbox — run these on a Docker host):
  1. Secrets: create a .env with dev values for BETTER_AUTH_SECRET, GOOGLE_CLIENT_ID/SECRET,
     APP_URL=http://localhost:3000, SMTP_URL=smtp://mailpit:1025, NODE_ENV=development, and the
     DATABASE_URL/APP_RUNTIME_DATABASE_URL the compose file expects (see docker-compose.yml + DEPLOYMENT.md).
  2. `docker compose up -d --build` (postgres 16 + mailpit + app). The app entrypoint runs
     init-db.sql (creates app_runtime) -> db:migrate -> boots Fastify on :3000 serving API + SPA.
  3. Health: `curl -fsS http://localhost:3000/api/health` (and /api/ready).
  4. `pnpm seed` (dev users + a team org + sample workspace/tasks; refuses if NODE_ENV=production).
  5. Isolation suite (RLS + cross-tenant + migration idempotency, F065-F069):
     TEST_DATABASE_URL=<owner> TEST_RUNTIME_DATABASE_URL=<app_runtime> pnpm test:isolation
  6. E2E: Playwright against http://localhost:3000 — sign-up -> verify via Mailpit (:8025) -> sign-in;
     create org -> invite (Mailpit) -> accept -> role change; workspace/task CRUD + assignee +
     maxTasks upgrade notice; CSV export -> Mailpit; rebrand smoke (swap product.config).
  7. Then mark the 13 deferred features per real results, and run /review (+ /security-review), /deploy.
  NB: sprint-contracts/ are empty (never generated) — either author them from features.json +
  specs/design/api-contracts.md, or evaluate directly against the feature ACs + this runbook.
next_action: run the EVALUATION RUNBOOK on a Docker host; report results back to mark deferred features.

================================================================================
LIVE EVALUATION RESULTS  (2026-07-19, Docker host: user's Windows machine)
================================================================================
Stack brought up live: Postgres 16 + Mailpit + production app image (built &
booted). Entrypoint chain verified: init-db.sql -> db:migrate -> Fastify on :3000
serving API + SPA. /api/health + /api/ready = 200.

RLS/isolation (F065-F069): 21/21 green against owner + app_runtime connections.
  - Fixed a test-probe defect: DELETE-org cross-tenant probe sent no body, so
    schema validation (400) fired before the authz guard; gave it a valid
    confirmationName body. (commit f954fc9)
  - Verified live: FORCE RLS on the 4 tenant tables; app_runtime NOBYPASSRLS +
    non-super + CRUD-only grants; owner BYPASSRLS.

Job queue (F022, F091): pg-boss processed email-delivery (6) + workspace.export
(2). Verification/invite/reset/export emails delivered to Mailpit, brand
"Scaffold Reference". Export email carried a valid inline CSV (header + rows).

Browser E2E (evaluator agent, real Chromium): sign-in PASS; sign-up + Mailpit
verification + verification-gate PASS; add-member + role-change PASS; task
create/complete/delete PASS; CSV export delivered PASS.

DEFERRED FEATURES RE-MARKED against real evidence -> now 93/95 passing:
  PASS(11): F012 F013 F014 F022 F065 F066 F067 F068 F069 F091 F093
  STILL FAILING - genuine gaps (2):
    F021  no cron scheduled at boot (pgboss.schedule empty; no registration).
    F082  no OpenAPI endpoint (/openapi.json + /docs are SPA fallback).

CONFIRMED DEFECTS found by live E2E:
  [FIXED] Invite-accept email pointed at /orgs/:id/invitations/:id/accept, not a
          registered SPA route -> client rendered "Page not found". Now emits
          /accept-invite?invitationId=<id>. Verified live via Mailpit. (796c106)
  [OPEN]  Admin-created-member set-password email points at /auth/set-password
          ?email= (no SPA route) AND mints no token; the /reset-password screen
          needs a token. Deeper fix: identity port must mint a reset token.
  [OPEN]  Workspace list has no affordance to open a workspace's tasks; the task
          view is reachable only by direct URL. UX gap.
  [OPEN-minor] No task-edit UI; no primary sign-out (only "sign out other
          sessions"); no in-UI CSV-export button (backend endpoint works).

EVALUATOR FALSE ALARMS (verified against code+live, NOT defects):
  - "maxTasks not enforced": default limit is 100; only ~76 tasks created.
    Enforcement (task.service.ts AC3 + upgrade-notice.tsx) exists.
  - "assignee dropdown empty": members endpoint returns members and the client
    unwraps .items correctly; evaluator's session was 429 rate-limited.

LESSON (LR candidate): server-side URL builders and SPA routes were each unit-
tested in isolation (server asserted the URL string; router asserted the route
existed) but nothing asserted they MATCH -> two broken onboarding links shipped.
Add a cross-check test (or a shared route constant) binding emailed URLs to
registered SPA routes.

NEXT: decide fix-now vs backlog for the OPEN items (esp. set-password token flow
+ workspace->tasks nav), then /review (+ /security-review) and /deploy.
